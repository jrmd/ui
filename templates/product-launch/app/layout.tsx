import './globals.css';import type {Metadata} from 'next';export const metadata:Metadata={title:"Afterhours — Product launch",description:"A cinematic WebGL launch with a playable sound-design instrument."};export default function Layout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}